#include "file.h"

/* TODO: permission control */
int checkDirectory(char* path) {
  if( path == NULL ) return 0;
  return 1;
}

int checkFile(char* path) {
  if( path == NULL ) return 0;
  return 1;
}